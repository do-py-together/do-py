{
    "name": "do-py",
    "description": "Standardized data structures for Python.",
    "version": "0.0.2",
    "main": "index.js",
    "repository": "git@github.com:do-py-together/do-py.git",
    "author": "Tim Davis <author@example.com>",
    "license": "MIT",
    "scripts": {
        "test": "pytest -svo xfail_strict=True --durations 10 --maxfail 10 --cov ./ --cov-report html --cov-report xml --junitxml test-reports/tests.xml ./tests/",
        "build": "python setup.py sdist bdist_wheel",
        "release-test": "twine upload --repository-url https://test.pypi.org/legacy/ dist/*",
        "release": "twine upload dist/*"
    }
}
